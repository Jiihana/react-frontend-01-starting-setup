class PlaceClass {
  constructor(id, title, imageUrl, description, adress, creatorId, coordinates) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.description = description;
    this.adress = adress;
    this.creatorId = creatorId;
    this.coordinates = coordinates;
  }
}

export default PlaceClass;
